#pragma once
// Multiple places depend on RenderTexture, so its definition is here.

/** An opaque handle representing the renderer-specific data for a texture. */
typedef struct RenderTexture RenderTexture;
